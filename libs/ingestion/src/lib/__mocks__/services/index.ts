/**
 * Index file for mock service implementations
 */

export * from './mock-content-storage.service';
// Note: mock-social-media.service.ts implementation doesn't exist yet,
// only the spec file exists
